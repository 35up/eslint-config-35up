# eslint-config-35up-svelte

This package provides 35up's Svelte .eslintrc as an extensible shared config.

## Usage

### Installation

  ```sh
  $ npm install @35up/eslint-config-35up-svelte --save-dev
  ```

### Setup

Add `"extends": "@35up/35up-svelte"` to your .eslintrc.
